name: mikutter
description: mikutter for flutter
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  http: ^0.13.0
  state_notifier: ^0.7.0
  flutter_state_notifier: ^0.7.0
  freezed_annotation: ^0.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable:
  build_runner:
  freezed:

flutter:
  uses-material-design: true
  assets:
    - assets/etc.png
